<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.mainmodule">

    <uses-permission android:name="android.permission.RECV_LCX"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ViewDragerHelperActivity">
        </activity>
        <activity android:name=".testmvp.TestMvpActivity">

        </activity>
        <activity android:name=".TestKeyBoardActivity">

        </activity>
        <activity android:name=".testDump.LeakActivity">
        </activity>
        <activity
            android:process=".task1"
            android:name=".multiProcessSharedPreferences.MultiProcessSharedActivity">
        </activity>
    </application>

</manifest>